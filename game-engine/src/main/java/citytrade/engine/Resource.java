package citytrade.engine;

/** The four tradeable resources. Money is not a resource (it has no storage limit and no upkeep use). */
public enum Resource {
    FOOD,
    ENERGY,
    MATERIALS,
    TECHNOLOGY
}
