package citytrade.server.ws;

/** The WebSocket message shape version (Architecture 6.9), independent of {@code rulesetVersion}. */
final class ProtocolVersion {

    static final int CURRENT = 1;

    private ProtocolVersion() {
    }
}
