@AGENTS.md

Claude Code note: your role (IMPLEMENTER or REVIEWER) is given in the prompt by
`scripts/run-until.sh` / `scripts/review.sh`. Follow `docs/IMPLEMENTER.md` or
`docs/REVIEW.md` accordingly. When the owner talks to you directly in a chat,
act as the implementer unless asked otherwise.
